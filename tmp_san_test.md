# Detecting hallucinations in large language models using semantic entropy

## 1 一、要解决的问题

### 1.1 大语言模型幻觉与“虚构”问题的定义及危害

大语言模型（LLM）如 ChatGPT、Gemini 在推理和问答中表现突出，但经常会产生幻觉（hallucination），即生成虚假或无根据的输出。论文将幻觉中一类特定子集定义为 **confabulation**（虚构/编造）：模型流畅地生成既错误又任意的内容，其答案对无关细节（如随机种子）敏感。例如，询问医学问题“Sotorasib 的靶点是什么？”模型有时回答 KRASG12C（正确），有时回答 KRASG12D（错误），尽管指令完全相同。

论文明确指出需要将 confabulation 与其他错误机制区分开：
- **训练数据错误**导致的系统性错误（如常见误解）；
- 模型为追求奖励而“撒谎”；
- 系统性的推理或泛化失败。

这些不同机制被笼统归入“幻觉”并不利于问题解决。本文方法只针对 confabulation，不保证事实性，但能大幅提升当前最先进 LLM 的问答准确率，说明 confabulation 是当前错误的主要来源之一。实际危害包括伪造法律先例、新闻失实、医疗领域（如放射学）威胁生命等。因此，需要一种通用方法检测 LLM 何时可能产生 confabulation，尤其是对人类也不知道答案的新问题有效。

### 1.2 自由形式生成中不确定性估计的核心挑战

传统熵等不确定性度量在自由形式文本生成中失效，原因在于：同一个含义可以有许多不同的词面表达。如果直接基于 token 序列概率计算熵（朴素熵），即使模型对答案含义非常确定，只要措辞有变化，熵也会被误导性地估计得很高。例如，“Paris”“It’s Paris”“France’s capital Paris”三个回答含义相同，但词面完全不同，朴素熵会认为模型非常不确定。

因此，需要将不确定性从 **token 序列层面** 提升到 **语义/含义层面**：计算模型对答案“意义”的不确定性，而不是对具体措辞的不确定性。过去关于 LLM 不确定性的研究多聚焦于分类器、回归器等封闭词汇任务，无法直接适用于自由形式生成这一最有吸引力的应用场景。

### 1.3 本文要解决的具体任务与评估场景

论文在两类任务上验证方法：
- **句子级自由形式问答与数学**：TriviaQA（常识问答）、SQuAD 1.1（阅读问答）、BioASQ（生命科学问答）、NQ-Open（开放域自然问题，来自 Google 搜索真实查询）、SVAMP（小学数学文字题）。
- **段落级传记生成**：新构建的 FactualBio 数据集，由 GPT-4 生成的较长传记段落，包含大量事实声明。

评估指标包括：
- **AUROC**：二分类事件（答案是否错误）的 ROC 曲线下面积，衡量检测错误的能力；
- **AURAC**（area under rejection accuracy curve）：在根据不确定性分数拒绝一部分最不确定问题后，剩余问题上的问答准确率曲线下面积，反映用户实际体验到的准确率提升。

## 2 二、前人方案及其不足

### 2.1 基于 token 概率的朴素熵

**做法**：直接计算生成 token 序列的联合概率（通常长度归一化）的熵，作为模型不确定性的度量。给定上下文 $x$，输出序列 $s$ 的联合概率为
$$
\log P(s|x) = \sum_i \log P(s_i | s_{<i}, x)
$$
长度归一化后计算预测熵：
$$
PE(x) = -\sum_y P(y|x) \ln P(y|x)
$$

**不足**：朴素熵将“语义不确定性”和“措辞不确定性”混为一谈。同一个含义可以有大量不同表达，模型对含义确定时，词面层面的熵仍然可能很高。因此它在检测 confabulation 时表现很差：论文平均 30 个任务/模型组合中，朴素熵 AUROC 仅 0.691，远低于语义熵的 0.790。它无法识别“含义不变、形式变化”的情况，会大量误报。

### 2.2 监督式嵌入回归

**做法**：受 Kadavath 等人 P(IK) 方法启发，利用 LLM 最后一层隐藏状态作为特征，训练一个 logistic 回归分类器，预测模型是否能够正确回答给定问题。它需要一组带有正确/错误标签的训练数据。

**不足**：该方法属于监督学习，假设训练和部署的数据分布一致。在分布偏移（out-of-distribution）时性能明显下降。论文显示，嵌入回归在分布内表现尚可，但在分布外（用其他数据集训练）时 AUROC/AURAC 大幅下降。而实际应用中，可靠的不确定性在分布偏移时最为重要。此外，它需要标注数据和模型内部嵌入，无法处理黑盒模型。

### 2.3 P(True) 少样本方法

**做法**：首先采样多个候选答案，然后将其与最可能答案一起作为上下文，让 LLM 判断“该答案是否为 True”，置信度分数取模型预测 next token 为 ‘True’ 的概率。该方法可通过在提示中加入最多 20 个 few-shot 训练示例来提升性能，因此是一种“上下文监督”方法。

**不足**：P(True) 依赖于模型自身的判断能力，且需要训练样例。其性能受模型规模影响较大：在较小模型（如 7B）上表现不佳，随模型变大有所改善，但论文指出它可能在“模型非常强且理解任务”的设置下才更有竞争力，而这些设置恰恰不是最需要不确定性估计的场景。此外，P(True) 受上下文长度限制，当问题较长或生成数量较多时，无法容纳完整的 20 个 few-shot 示例。平均 AUROC 为 0.698，低于语义熵。

### 2.4 简单自检与其他相关方法

**做法**：自检（self-check）基线直接询问 LLM：“该陈述是否可能为真？” 用模型回答 “yes/no” 的比例作为置信度。

**不足**：论文在 FactualBio 段落级实验中发现，自检基线表现远不如语义熵。这反驳了“模型大多知道自己不知道什么”的观点，说明模型虽然有隐式知识，但无法直接通过自检将其表达出来。更广泛的不确定性估计工作（针对分类器、回归器）则不能直接用于自由形式生成。

## 3 三、本文方案及其优点

### 3.1 核心思想：在语义层面计算熵

论文提出 **语义熵（semantic entropy）**，将 LLM 输出空间从 token 序列提升到 **语义等价类**。高语义熵表示模型对答案的 **含义** 不确定，很可能是 confabulation；低语义熵表示模型对含义有信心，即使措辞变化很大。

方法分三步：
1. **生成**：从 LLM 预测分布中采样多个输出序列；
2. **聚类**：根据双向蕴含（bidirectional entailment）将语义相同的序列聚为一类；
3. **熵估计**：计算语义簇分布上的熵，得到语义熵。

直觉：如果模型真正知道答案，即使每次采样措辞不同，语义聚类后所有答案会落入同一簇，熵低；如果模型在随意编造，答案语义会分散到多个簇，熵高。

> **追问**：语义熵是什么
>
> **解答**：语义熵（semantic entropy）是一种在**含义层面**而非具体措辞层面度量 LLM 不确定性的方法。它先让模型对同一问题采样多个答案，再用**双向蕴含**把语义相同的答案聚为一类，最后计算这些语义簇分布上的熵：
> 
> $$
> SE(x) \approx -\sum_i P(C_i|x)\log P(C_i|x)
> $$
> 
> 其中 $P(C_i|x)$ 是该语义簇的概率（可由 token 概率求和或样本比例估计）。语义熵高，说明模型对答案的“意义”本身不确定，输出很可能是任意编造的 **confabulation**；语义熵低，则说明即使措辞多样，模型对含义仍自信。相比只比较词面差异的**朴素熵**，它能避免“Paris / It’s Paris / France’s capital Paris”这类同义表达被误判为高不确定。

### 3.2 标准语义熵的估计公式与算法

预测熵的定义为：
$$
PE(x) = -\sum_y P(y|x) \ln P(y|x)
$$
其中 $x$ 是输入上下文，$y$ 是输出。

对生成序列 $s$，其条件概率为
$$
\log P(s|x) = \sum_i \log P(s_i | s_{<i}, x)
$$
其中 $s_i$ 是第 $i$ 个输出 token，$s_{<i}$ 是前面的 token。

为避免长句主导熵，采用长度归一化：
$$
\frac{1}{N} \sum_i \log P(s_i | s_{<i}, x)
$$

引入语义等价类 $c$，一个语义类 $c$ 的概率是其中所有可能序列的概率之和：
$$
P(c|x) = \sum_{s \in c} P(s|x)
$$

语义熵定义为
$$
SE(x) = -\sum_c P(c|x) \log P(c|x)
$$

由于无法枚举所有语义类，论文使用 Rao–Blackwellized 蒙特卡洛估计。采样 $M$ 个序列并聚类得到 $C_1, \dots, C_{|C|}$，然后估计：
$$
SE(x) \approx -\sum_i P(C_i|x) \log P(C_i|x)
$$
其中
$$
P(C_i|x) = \frac{\sum_{c \in C_i} P(c|x)}{\sum_j \sum_{c \in C_j} P(c|x)}
$$
此归一化步骤防止长度归一化导致簇概率之和超过 1。

**算法步骤**（对应 Extended Data Fig. 1）：
1. 输入上下文 $x$，采样 $M$ 个输出序列 $\{s^{(1)}, \dots, s^{(M)}\}$，记录每个序列的 token 概率。
2. 初始化空簇集合。对于每个新生成的序列，与已有簇中的任意一个序列（通常取该簇第一个）进行双向蕴含检测。如果双向蕴含成立，则加入该簇；否则新建一个簇。
3. 对每个簇 $C_i$，计算 $P(C_i|x)$。
4. 计算语义熵 $SE(x) \approx -\sum_i P(C_i|x) \log P(C_i|x)$。

### 3.3 离散语义熵变体

当无法获得 token 概率或模型内部状态时（例如早期 GPT-4 只提供黑盒输出），论文提出 **离散语义熵（discrete semantic entropy）**。它直接从生成样本的簇分布中估计类别概率，忽略 token 概率：

$$
P(C_i|x) \approx \frac{1}{M} \sum_{s \in C_i} 1
$$

即某一语义簇的概率近似等于该簇中生成样本数占总样本数 $M$ 的比例。这相当于假设每个实际生成的输出是等概率的，即使用经验类别分布。

当 $M \to \infty$ 时，由大数定律，该估计收敛于标准语义熵估计。论文实验表明，离散语义熵的性能与标准语义熵非常接近，尽管它不要求访问精确输出概率。在 GPT-4 段落级实验和部分黑盒场景中，离散变体是必需选择。

### 3.4 语义等价聚类与双向蕴含

语义等价通过 **双向蕴含** 来操作性定义：两个句子 $s$ 和 $s'$ 语义相同，当且仅当它们互相蕴含（entail）。例如“法国首都是巴黎” 蕴含 “巴黎是法国首都”，反之亦然。

关键点是语义等价依赖于上下文：例如 “巴黎” 本身不是句子，但在问题 “法国首都是什么？” 的上下文中，它蕴含 “法国首都是巴黎”。因此聚类时需要使用问题和答案拼接进行蕴含判断。

实现方式：
- **NLI 模型**：DeBERTa-large 微调于 MNLI，参数量 1.5B，相对轻量；
- **通用 LLM 提示**：指令微调模型如 GPT-3.5，使用提示 “Does Possible Answer 1 semantically entail Possible Answer 2? Respond with entailment, contradiction, or neutral.”

论文在句子级实验中最终采用 GPT-3.5 进行蕴含预测，因为其与人类判断一致性更好；在段落级实验中发现 DeBERTa 在短答案上效果更好，并采用 “非矛盾单向蕴含” 策略（即至少一个方向蕴含且不存在矛盾就视为相同含义）以提高召回。

聚类算法利用等价关系的传递性，只需检查新句子与已有簇内任意一句的双向蕴含即可。

### 3.5 句子级问答与数学任务中的实验设置与结果

**任务与数据**：使用 BioASQ、SQuAD 1.1、TriviaQA、SVAMP、NQ-Open 五个数据集，每个数据集随机选取 400 训练 / 400 测试样本（仅监督方法使用训练集，语义熵不使用）。所有任务均为自由形式生成，答案平均长度 96 ± 70 字符。

**模型**：LLaMA 2 Chat（7B/13B/70B）、Falcon Instruct（7B/40B）、Mistral Instruct（7B）。生成参数：温度 1，核采样 p=0.9，top-K=50；另采样温度 0.1 的单个输出作为“最佳答案”用于准确性评估。熵计算使用 10 次生成（由 Supplementary Fig. 2 分析确定）。

**评估**：答案正确性由 GPT-4 与参考答案比较确定（提示词见 Methods）；AUROC 和 AURAC 在留出测试集上计算。

**结果**：在 30 个任务/模型组合的平均值上，语义熵 AUROC 为 **0.790**，离散语义熵与之接近；朴素熵 0.691，P(True) 0.698，嵌入回归 0.687。语义熵在不同模型家族和规模间表现稳定，AUROC 均在 0.78–0.81 之间。

AURAC 同样显示语义熵优于所有基线：通过拒绝语义熵高的问题，模型在剩余问题上的准确率明显提升。论文指出，嵌入回归在分布偏移（训练和部署数据集不同）时性能显著下降，而语义熵不受影响。

代表性实例（简化自 Table 1）：

| 问题 | 参考/模型答案特征 | 语义熵预测 | 朴素熵预测 |
|---|---|---|---|
| 工业部门分类问题 | 多次生成措辞不同但含义相同（“工业部门”） | 非 confabulation（正确） | confabulation（错误） |
| STARD10 蛋白功能 | 多次生成含义完全不同 | confabulation（正确） | confabulation（正确） |
| Fado 是哪个国家的民族音乐 | 多次生成几乎相同 | 非 confabulation（正确） | 非 confabulation（正确） |
| BSkyB 数字服务启动日期 | 生成中同时出现“1998”和“1 October 1998”，语义聚类过度区分 | confabulation（误报） | 非 confabulation（正确） |

第四行表明语义聚类对日期粒度过度敏感可能导致误报，这需要上下文判断和更细致的语义等价定义。

### 3.6 段落级传记生成中的扩展方法与结果

**FactualBio 数据集**：随机从 WikiBio 采样 21 位有维基百科页面但缺乏详细在线传记的人物，由 GPT-4 (v.0613) 生成传记。使用 GPT-4 自动提取事实声明，共 150 个事实命题（45 个为错误），人工逐一标注真伪。

**扩展方法**：不能直接逐句重采样，因为模型可能在不同次生成中改变叙述顺序或侧重不同事实，导致语义熵被顺序和结构的不确定性污染。因此采取以下流程：
1. 将段落自动分解为若干事实声明；
2. 对每个事实声明，使用 LLM 生成 6 个可能以该事实为答案的问题（分两次提示，每次生成 3 个问题，以提高覆盖度）；
3. 对每个问题，用原模型生成 3 个新答案；
4. 计算这些答案（加上原事实声明）的语义熵，并取平均值作为该事实的 confabulation 分数；
5. 如果超过半数答案包含 “not available” “not provided” “unknown” “unclear” 等拒绝性表述，则将该事实的语义不确定性置为最大。

**结果**：由于当时 GPT-4 不提供输出概率，使用离散语义熵。Fig. 3 显示离散语义熵的 AUROC 和 AURAC 均高于两个基线：自检基线和 P(True) 变体。在拒绝前 80% 问题的区间内，语义熵的拒绝准确率最高；仅当拒绝 top 20% 最不确定事实后，P(True) 变体在剩余子集上的准确率略高于语义熵。这表明语义熵能有效识别段落中的任意编造事实。

### 3.7 方法优点与局限总结

**优点**：
- **无监督、无需任务特定数据**：语义熵不需要任何标注样例，可跨数据集和任务泛化；
- **语义层面而非词面层面**：解决了自由文本多表达导致的假性高不确定性问题；
- **适用于黑盒模型**：离散变体只需模型输出，无需 token 概率或隐藏状态；
- **性能稳定**：在多个模型家族（LLaMA 2、Falcon、Mistral）和规模（7B 到 70B）上保持 AUROC 0.78–0.81；
- **可扩展至长文本**：通过事实分解 + 问题重构 + 语义熵平均，成功应用于段落级传记生成。

**局限**：
- 只针对 confabulation（任意错误），不处理系统性错误、训练数据偏差、奖励驱动谎言等；
- 语义聚类依赖于蕴含判断的准确性，可能因上下文粒度（如日期具体性）导致误判；
- 计算成本较高：需多次采样、多次蕴含判断；
- 无法保证事实性，只是检测可能的任意编造。

## 4 四、前景与发展方向

### 4.1 扩展到更多自然语言生成任务

语义熵目前主要应用于自由形式问答、数学文字题和传记生成。论文预计，将语义不确定性思想进行算法适配，可以在 **抽象式摘要、机器翻译、对话系统** 等任务中取得类似进展。不同任务对“语义等价”的定义可能不同，需要针对性地设计聚类策略。

### 4.2 结合其他不确定性度量与认知/偶然不确定性分解

当前语义熵使用预测熵作为不确定性度量。论文指出，可以将语义熵中的熵替换为互信息等其他度量，以捕捉不同类型的不确定性。进一步地，如果能够分离 **aleatoric uncertainty**（数据固有不确定性）与 **epistemic uncertainty**（知识不足导致的不确定性），将有助于更精细地检测 confabulation 并理解其来源。

### 4.3 基于输入扰动的交叉检验与辩论

论文提出，可以扩展语义熵方法至输入扰动场景，例如对问题进行改写或引入反事实条件，观察输出语义是否保持一致。这可以作为一种 **交叉检验（cross-examination）**，支持可扩展监督（scalable oversight）和辩论（debate）机制。语义熵可以作为衡量模型在面临不同输入变体时是否一致的工具。

### 4.4 处理系统性错误与非 confabulation 幻觉

语义熵明确不解决模型因训练数据错误、系统性推理缺陷或奖励函数误导而导致的“自信错误”。这些错误虽然症状相似（都输出错误内容），但底层机制不同，需要单独研究。未来的一个方向是识别和区分这些错误模式，并将语义熵与其他机制检测方法结合，以构建更全面的可信度评估体系。

### 4.5 改进语义聚类与计算效率

当前方法中，语义等价判断依赖 NLI 模型或 LLM 提示，存在两方面改进空间：
- **语义聚类的粒度**：如日期具体性（“1998” vs “1 October 1998”）在一些上下文中可能并不重要，需要研究更灵活的“软等价”或上下文相关聚类；
- **计算成本**：多次采样和两两蕴含判断开销很大，未来可以研究减少生成数量、使用更轻量的蕴含检测器、近似聚类等方法来提升效率，使语义熵能在实时系统中应用。

