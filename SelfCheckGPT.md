# SelfCheckGPT: Zero-Resource Black-Box Hallucination Detection for Generative Large Language Models

## 1 一、要解决的问题

### 1.1 生成式大语言模型的幻觉检测难题

生成式大语言模型（LLMs）如 GPT-3、PaLM 能够针对各种用户提示生成高度流畅、逼真的回复文本，已广泛应用于报告起草、虚拟助手和摘要系统等场景。然而，LLMs 被广泛观察到会“幻觉”（hallucinate）事实——即自信地生成虚构信息。这种幻觉现象严重损害了用户对模型输出的信任，并且令人担忧的是，目前几乎没有合适的方法来识别 LLM 的幻觉。

本文旨在解决的核心问题可以形式化定义为：**给定一个黑盒 LLM 生成的一段文本响应 $R$，在不访问模型内部状态、不依赖外部知识库的条件下，判断其中每个句子 $r_i$ 是否为幻觉（非事实性）陈述，并进一步对整个篇章的事实性进行排序评估**。具体包括两个粒度：

- **句子级幻觉检测**：对响应 $R$ 中的第 $i$ 个句子，预测一个幻觉得分 $S(i) \in [0.0, 1.0]$，其中 $S(i) \to 0.0$ 表示该句子基于有效信息（事实正确），$S(i) \to 1.0$ 表示该句子是幻觉。
- **篇章级事实性排序**：通过对句子级得分取平均得到篇章级得分 $S_{passage} = \frac{1}{|R|} \sum_i S(i)$，用于对多段生成文本进行事实性排名。

### 1.2 黑盒与零资源双重约束

本文特别强调两个实际约束条件：

- **黑盒约束（Black-box）**：许多现代 LLM（如 ChatGPT）仅通过受限的外部 API 提供服务，用户**无法获取 token 级概率分布或内部隐藏状态**。这排除了基于输出概率或内部表示的检测方法。
- **零资源约束（Zero-resource）**：不依赖外部数据库进行事实核查。传统事实核查需要检索外部证据，但事实只能相对于数据库中已有知识进行评估，且幻觉出现在远超纯事实核查的广泛任务中（如摘要、对话），不可能为每种用例都维护一个外部知识库。

### 1.3 本文方法要满足的设计目标

在上述约束下，本文方法需要：
1. 仅使用文本响应和随机抽样样本，不访问概率分布或内部状态；
2. 无需外部数据库；
3. 能够同时完成句子级幻觉检测和篇章级事实性排序；
4. 检测性能优于灰盒（grey-box）基线方法。

## 2 二、前人方案及其不足

### 2.1 基于Token概率的灰盒不确定性方法

**做法**：利用预训练 LLM 在推理时输出的 token 概率来衡量模型对生成内容的不确定性。对于响应 $R$ 的第 $i$ 个句子的第 $j$ 个 token，记其生成概率为 $p_{ij}$，句子长度为 $J$，定义两类概率度量：

$$
Avg(-\log p) = -\frac{1}{J} \sum_j \log p_{ij}
$$

$$
Max(-\log p) = \max_j (-\log p_{ij})
$$

其中 $Max(-\log p)$ 通过评估句子中概率最低的 token 来衡量整句的不确定性。

**不足**：这类方法要求**完全访问输出概率分布**，即必须是灰盒（grey-box）甚至白盒设置。然而，对于通过受限 API 调用的黑盒系统（如 ChatGPT），用户无法获得 token 级概率信息，该方法完全无法使用。此外，即使概率可用（如 GPT-3 API 返回 top-5 token 概率），熵的计算也可能因信息不完整而不够准确。

### 2.2 基于熵的灰盒不确定性方法

**做法**：计算输出分布的熵。第 $i$ 个句子中第 $j$ 个 token 的输出分布熵定义为：

$$
H_{ij} = -\sum_{\tilde{w} \in W} p_{ij}(\tilde{w}) \log p_{ij}(\tilde{w})
$$

其中 $p_{ij}(\tilde{w})$ 是词 $\tilde{w}$ 在该位置被生成的概率，$W$ 是词汇表。与概率度量类似，定义两种熵度量：

$$
Avg(H) = \frac{1}{J} \sum_j H_{ij}; \quad Max(H) = \max_j(H_{ij})
$$

**不足**：与概率方法相同，熵的计算**需要完整或至少 top-k 的输出分布**。本文实验中 GPT-3 API 仅返回 top-5 token 的概率，导致熵的信息不完整。此外，实验表明 GPT-3 自身的熵度量（top-5）性能低于概率度量，说明 top-5 熵的信息量不足。

### 2.3 基于外部数据库的事实核查方法

**做法**：传统自动事实核查（如 FEVER 相关研究）遵循多阶段流水线：声明检测（claim detection）、证据检索（evidence retrieval）和裁决预测（verdict prediction）。Guo et al.（2022）和 Zhong et al.（2020）等综述和系统总结了这一框架。

**不足**：第一，**必须访问外部数据库**，而事实只能相对于数据库中已有知识进行评估，导致覆盖范围有限；第二，这些模块通常通过独立且复杂的组件连接，**推理成本相当高**；第三，LLM 幻觉出现在摘要、对话等广泛任务中，远超纯事实核查的范畴，为每种用例维护外部数据库在实践中不可行。

### 2.4 白盒监督学习方法

**做法**：Azaria and Mitchell（2023）训练一个多层感知器（MLP）分类器，将 LLM 的**隐藏表示**作为输入，预测句子的真实性。这是一种白盒方法。

**不足**：第一，需要访问 LLM 的**内部隐藏状态**，这在 API 调用场景中不可用；第二，需要**标注数据进行监督训练**，成本高昂且标注数据本身存在主观性问题；第三，该方法是模型特定的，泛化性差。

### 2.5 自我评估方法

**做法**：Kadavath et al.（2022）提出自我评估（self-evaluation），让 LLM 被提示来评估其前一次预测的正确性，例如预测其生成的响应/答案为真的概率。

**不足**：LLM 对自己输出的评估能力有限，且容易被提示的措辞影响。论文中没有将该方法作为主要基线对比，表明其在幻觉检测任务上的可靠性尚未得到充分验证。

### 2.6 代理LLM方法

**做法**：使用一个用户**完全可访问**的代理 LLM（如 LLaMA-30B）来近似黑盒 LLM 的输出 token 概率。具体而言，将被评估的文本输入代理 LLM，获取每个 token 的条件概率，然后计算上述概率或熵度量。

**不足**：实验结果（表 2）显示代理 LLM 的性能明显差于生成文本的原 LLM（GPT-3）。例如，LLaMA-30B 的 $Avg(-\log p)$ 在事实性句子检测上的 AUC-PR 仅为 41.29，远低于 GPT-3 的 53.97；在篇章级 Pearson 相关性上仅为 21.72。使用其他代理 LLM（如 GPT-NeoX-20B、OPT-30B）时性能接近随机基线。原因在于**不同 LLM 具有不同的生成模式**，即使在响应与代理 LLM 生成风格不相似的情况下，常见 token 也可能被分配低概率。论文指出，加权条件语言模型分数（如 BARTScore）可能是未来改进方向。

## 3 三、本文方案及其优点

### 3.1 SelfCheckGPT总体框架与核心思想

SelfCheckGPT 是一种基于抽样的黑盒零资源幻觉检测方案，其核心直觉是：**如果 LLM 对某个概念具有知识，那么随机抽样的多个响应很可能相似且包含一致的事实；而对于幻觉事实，随机抽样的响应很可能发散并相互矛盾**。

具体操作流程为：给定 LLM 对用户查询的一个主响应 $R$，使用相同查询进一步随机抽取 $N$ 个响应样本 $\{S_1, S_2, ..., S_n, ..., S_N\}$，然后测量 $R$ 中的每个句子 $r_i$ 与样本之间的一致性。通过一致性测量来预测幻觉得分 $S(i)$。

SelfCheckGPT 具有以下显著优点：
- **纯黑盒**：仅利用采样文本响应，不需要 token 概率或内部状态；
- **零资源**：不依赖任何外部数据库；
- **通用性**：适用于任何黑盒 LLM 系统；
- **简单性**：核心操作仅为随机抽样和一致性比较，易于实现。

### 3.2 变体一：SelfCheckGPT-BERTScore

**做法**：使用 BERTScore 测量 $R$ 中第 $i$ 个句子 $r_i$ 与每个抽样样本 $S_n$ 中最相似句子的语义相似度。具体公式为：

$$
S_{BERT}(i) = 1 - \frac{1}{N} \sum_{n=1}^{N} \max_k \big(B(r_i, s^n_k)\big)
$$

其中 $B(\cdot, \cdot)$ 表示两个句子之间的 BERTScore，$r_i$ 是响应 $R$ 中的第 $i$ 个句子，$s^n_k$ 是第 $n$ 个样本 $S_n$ 中的第 $k$ 个句子。

**直觉**：如果句子中的信息出现在多个抽样样本中，则假设该信息是事实性的；如果该陈述没有在任何其他样本中出现，则很可能是幻觉。

**实现细节**：使用 RoBERTa-Large 作为 BERTScore 的主干模型。

**实验结果**：该变体在非事实句子检测上 AUC-PR 为 81.96，在事实性句子检测上为 44.23，篇章级 Pearson 相关性 58.18，Spearman 相关性 55.90。虽然低于 Prompt 和 NLI 变体，但超过多数灰盒基线。

### 3.3 变体二：SelfCheckGPT-QA（基于MQAG问答框架）

**做法**：采用自动多项选择题生成（MQAG）框架来测量一致性。MQAG 由两个阶段组成：问题生成 $G$ 和问题回答 $A$。对于响应 $R$ 中的句子 $r_i$，首先根据 $r_i$ 和 $R$ 生成问题 $q$ 和选项 $o$：

$$
q, o \sim P_G(q, o | r_i, R)
$$

然后回答阶段 $A$ 分别基于主响应 $R$ 和每个样本 $S_n$ 选择答案：

$$
a_R = \arg\max_k \big[P_A(o_k | q, R, o)\big]
$$

$$
a_{S_n} = \arg\max_k \big[P_A(o_k | q, S_n, o)\big]
$$

比较 $a_R$ 与 $a_{S_n}$ 是否相等，统计匹配数 $N_m$ 和不匹配数 $N_n$，并定义简单的非一致性得分为 $S_{QA}(i, q) = \frac{N_n}{N_m + N_n}$。

**贝叶斯改进与软计数**：为考虑生成问题的可回答性，论文引入了基于贝叶斯定理的软计数改进。设句子为非事实（False）和事实（True）的先验概率相等，即 $P(F) = P(T)$，则：

$$
P(F|L_m, L_n) = \frac{P(L_m, L_n|F)}{P(L_m, L_n|F) + P(L_m, L_n|T)} = \frac{\gamma_2^{N_n}}{\gamma_1^{N_m} + \gamma_2^{N_n}}
$$

其中 $\gamma_1 = \frac{\beta_2}{1-\beta_1}$，$\gamma_2 = \frac{\beta_1}{1-\beta_2}$。作者取 $\beta_1 = \beta_2 = 0.8$。

再用可回答性分数 $\alpha_n$ 进行软计数替代硬计数：

$$
N'_m = \sum_{n \text{ s.t. } a_n \in L_m} \alpha_n; \quad N'_n = \sum_{n \text{ s.t. } a_n \in L_n} \alpha_n
$$

其中 $\alpha_n = P_U(\text{answerable} | q, S_n)$ 由在 SQuAD2.0 上微调的 Longformer 给出。最终得分为：

$$
S_{QA}(i) = \mathbb{E}_q \big[P(F|L_m, L_n)\big] = \frac{\gamma_2^{N'_n}}{\gamma_1^{N'_m} + \gamma_2^{N'_n}}
$$

**系统组成**：问题生成器 $G_1$（T5-Large 微调于 SQuAD）生成问题和对应答案，$G_2$（T5-Large 微调于 RACE）生成干扰项；回答系统 $A$ 为 Longformer 微调于 RACE；可回答性系统 $U$ 为 Longformer 微调于 SQuAD2.0。

**消融结果**（表 5）：简单计数 AUC-PR 为 83.97（NonFact）；加 Bayes 后为 83.04；再加软计数 $\alpha$ 后提升至 84.26，篇章级 Spearman 从 55.15 提升至 59.29。说明贝叶斯定理和软计数对性能有正面贡献。

**实验结果**：该变体在句子级非事实检测 AUC-PR 为 84.26，事实性检测为 48.14，篇章级 Pearson 61.07，Spearman 59.29。

> **追问**：MQAG是什么
>
> **解答**：MQAG（Multiple-choice question answering and generation）是一种自动评估信息一致性的框架，最初用于评估摘要的事实一致性。它包含两个阶段：**问题生成 G**（根据待评估句子生成多项选择题，包括问题、正确答案和干扰项）和**问题回答 A**（独立的问答系统基于不同上下文选择答案）。通过比较不同上下文下答案是否一致来判断信息是否被支持。
> 
> 在 SelfCheckGPT 中，MQAG 用作变体二（SelfCheckGPT-QA）：对主响应中的句子生成问题，分别用主响应和每个随机采样样本回答，统计匹配/不匹配数量，再结合贝叶斯软计数（考虑问题可回答性）计算幻觉得分。该变体在句子级检测上 AUC-PR 达 84.26，优于 BERTScore 变体，但低于 NLI 和 Prompt 变体。

> > **追问**：问题和选项具体是如何生成的
> >
> > **解答**：MQAG 的问题和选项采用两阶段生成：
> > 
> > 1. **问题与正确答案生成**：使用生成器 G1（T5-Large 微调于 SQuAD），以主响应中的句子 r_i 为输入，生成问题 q 及对应正确答案 a，即 \(q,a \sim P_{G1}(q,a|r_i)\)。
> > 
> > 2. **干扰项生成**：使用生成器 G2（T5-Large 微调于 RACE），基于问题 q、正确答案 a 和主响应全文 R，生成三个干扰项 \(o_{\backslash a}\)，即 \(o_{\backslash a} \sim P_{G2}(o_{\backslash a}|q,a,R)\)，最终形成四个选项 \(o=\{a, o_{\backslash a}\}=\{o_1,...,o_4\}\)。
> > 
> > 此外，用可回答性模型 U（Longformer 微调于 SQuAD2.0）计算问题可回答性分数 \(\alpha\)，用于过滤低质量问题或在贝叶斯软计数中加权。

> > > **追问**：这里的~数学符号是什么意思
> > >
> > > **解答**：这里的波浪号 \(\sim\) 表示“服从……分布”或“从……分布中采样”。
> > > 
> > > 例如：
> > > \[
> > > q,a \sim P_{G1}(q,a|r_i)
> > > \]
> > > 意思是：问题 \(q\) 和正确答案 \(a\) 是从条件概率分布 \(P_{G1}(q,a|r_i)\) 中采样得到的，即生成器 G1 在给定句子 \(r_i\) 的条件下，按照该分布随机生成问题和答案。
> > > 
> > > 同理，\(o_{\backslash a} \sim P_{G2}(o_{\backslash a}|q,a,R)\) 表示干扰项是从 G2 定义的分布中采样。因此 \(\sim\) 是描述随机变量来源的数学记号。

> > > > **追问**：我还是不理解q和a的生成方式。什么叫以一个句子为条件？这又是怎么得到一个随机分布的？
> > > >
> > > > **解答**：“以句子 r_i 为条件”指生成器把句子 r_i 作为输入，让生成结果依赖于它的内容。T5 是编码器-解码器模型：编码器先将 r_i 编码成语义向量，解码器再基于这个向量逐 token 生成问题和答案。
> > > > 
> > > > “随机分布”不是在句子层面直接造出来的，而是来自自回归解码。生成每个 token 时，模型都会输出词表上的概率分布 \(P(\text{token} \mid \text{已生成部分}, r_i)\)，然后从这个分布中**采样**下一个 token。每一步采样不同，最终就得到不同的 q,a 序列。因此 \(q,a \sim P_{G1}(q,a|r_i)\) 表示整个序列是从以 r_i 为条件的序列级分布中采样出来的。
> > > > 
> > > > 干扰项生成同理：G2 以问题 q、正确答案 a 和全文 R 为条件，自回归采样出三个干扰项。可回答性模型 U 也是类似地输出一个可回答性概率 \(\alpha\)。

> > > > > **追问**：一个句子r_i会生成几道选择题？
> > > > >
> > > > > **解答**：论文中并未明确给出每个句子 r_i 生成选择题的具体数量。从公式 \(S_{QA}(i)=\mathbb{E}_q[S_{QA}(i,q)]\) 来看，每个句子会生成多个问题 q，并对它们的不一致分数取平均；但“多少个”这一数字在论文正文和附录中均未指定。实际实现可能沿用了 MQAG（Manakul et al., 2023）的默认设置（例如每句生成 3 个问题），但本文未明确说明。如需确切数值，需查阅官方代码或 MQAG 原论文。

### 3.4 变体三：SelfCheckGPT-n-gram

**做法**：将抽样样本 $\{S_1, ..., S_N\}$ 作为训练语料，构建一个 n-gram 语言模型来近似生成这些响应的 LLM。当 $N$ 足够大时，该语言模型将收敛到原始 LLM。实际中由于样本数量有限，需要加入主响应 $R$ 作为平滑（将 $R$ 中每个 token 的计数增加 1）。然后计算被评估句子在 n-gram 模型下的负对数概率：

$$
S^{Avg}_{n\text{-}gram}(i) = -\frac{1}{J} \sum_j \log \tilde{p}_{ij}
$$

$$
S^{Max}_{n\text{-}gram}(i) = \max_j (-\log \tilde{p}_{ij})
$$

其中 $\tilde{p}_{ij}$ 是句子 $r_i$ 第 $j$ 个 token 在 n-gram 模型下的概率。

**直觉**：这种方法是灰盒概率方法的黑盒近似——通过抽样样本来估计 token 概率，从而在不访问实际概率分布的情况下重现不确定性度量。

**实验结果**：unigram 的 Max 变体在句子级 NonFact AUC-PR 达 85.63、Factual 达 58.47，篇章级 Pearson 64.71、Spearman 64.91。附录表 7 显示：
- **Max 优于 Avg**：unigram Max 的 NonFact AUC-PR 为 85.63，而 unigram Avg 仅为 81.52；篇章级 Pearson 分别为 64.71 和 40.68。
- **随着 n 增大，Max 性能下降**：2-gram Max 的 NonFact AUC-PR 为 85.26，5-gram 降至 84.12；篇章级 Pearson 从 64.71 降至 54.84。

**直觉解释**：Max 变体本质上是在评估句子时找出给定所有样本后出现次数最少的 token。如果某个 token 在 20 个样本中仅出现一两次，则很可能对应非事实信息。

### 3.5 变体四：SelfCheckGPT-NLI

**做法**：利用自然语言推理（NLI）模型判断被评估句子是否被样本段落所支持（entailment）或与之矛盾（contradiction）。使用在 MNLI 上微调的 DeBERTa-v3-large 作为 NLI 模型。

NLI 分类器的输入为“前提 + 假设”，即抽样段落 $S_n$ 与待评估句子 $r_i$ 的拼接。仅考虑 entailment 和 contradiction 两个类别的 logits，通过归一化排除 neutral 类：

$$
P(\text{contradict} | r_i, S_n) = \frac{\exp(z_c)}{\exp(z_e) + \exp(z_c)}
$$

其中 $z_e$ 和 $z_c$ 分别是 entailment 和 contradiction 类别的 logits。这种归一化确保概率限制在 $[0.0, 1.0]$ 区间内。最终的 SelfCheckGPT-NLI 得分为：

$$
S_{NLI}(i) = \frac{1}{N} \sum_{n=1}^{N} P(\text{contradict} | r_i, S_n)
$$

**直觉**：该方法直接检测矛盾——如果句子在多个样本中都被判定为矛盾，则很可能包含幻觉事实。

**实验结果**：该变体在句子级 NonFact AUC-PR 达 **92.50**，Factual AUC-PR 达 **66.08**，篇章级 Pearson 相关性 **74.14**、Spearman **73.78**。性能显著优于 BERTScore、QA 和 n-gram 变体，并接近 Prompt 方法。由于 SelfCheckGPT-Prompt 计算量较大，**NLI 变体被认为是性能与计算效率之间最佳折衷的实用方法**。

### 3.6 变体五：SelfCheckGPT-Prompt

**做法**：直接通过提示（prompting）让 LLM 评估第 $i$ 个句子是否被样本 $S_n$ 支持。使用的提示模板为：

```
Context: {}
Sentence: {}
Is the sentence supported by the context above?
Answer Yes or No:
```

初步调查显示 GPT-3（text-davinci-003）在 98% 的情况下会输出 Yes 或 No，剩余输出被置为 N/A。将输出映射为得分 $x_i^n$：{Yes: 0.0, No: 1.0, N/A: 0.5}。最终不一致性得分为：

$$
S_{Prompt}(i) = \frac{1}{N} \sum_{n=1}^{N} x_i^n
$$

**要点**：论文发现能力较弱的模型（如 GPT-3 text-curie-001 或 LLaMA）无法有效地通过此类提示进行一致性评估，因此需要足够强大的 LLM 作为评估器。

**实验结果**：该变体是**所有方法中性能最好的**——句子级 NonFact AUC-PR 达 **93.42**，NonFact* AUC-PR 达 **53.19**，Factual AUC-PR 达 **67.09**，篇章级 Pearson 相关性 **78.32**、Spearman **78.30**。显著优于灰盒方法（GPT-3 自身概率）和其他所有基线。

**GPT-3 自检与 ChatGPT 对比**（表 4）：GPT-3 可以自检自身生成的文本，仅用 4 个样本时 Spearman 达 74.69，优于 unigram 方法的 64.91；ChatGPT 作为评估器时略有提升（4 样本 Spearman 76.41），且 API 成本大幅降低（每 1000 token 为 $0.002 vs $0.020）。

### 3.7 数据集构建与标注

**数据生成**：从 WikiBio 测试集中按段落长度排名，随机抽取前 20% 最长文章中的 238 个概念（确保没有非常冷门的概念）。使用 GPT-3（text-davinci-003）以提示 "This is a Wikipedia passage about {concept}:" 生成维基百科式文章。主响应的生成采用温度 0.0 和标准束搜索解码；随机抽样样本采用温度 1.0，共生成 $N = 20$ 个样本。

| 统计量 | 数值 |
|--------|------|
| 篇章数 | 238 |
| 句子数 | 1908 |
| 每篇 token 数 | 184.7 ± 36.9 |

**句子级标注**：采用图 3 中的标注流程图，每个句子被分类为三类：
- **Major Inaccurate（非事实，1）**：句子完全为幻觉，与主题无关；
- **Minor Inaccurate（非事实，0.5）**：句子包含一些非事实信息，但与主题相关；
- **Accurate（事实，0）**：句子中呈现的信息准确。

**标注统计**：1908 个标注句子中，761 个（39.9%）被标注为 major-inaccurate，631 个（33.1%）为 minor-inaccurate，516 个（27.0%）为 accurate。201 个句子有两名标注者标注，使用 Cohen's κ 衡量标注者间一致性：3 类场景 κ = 0.595（中等一致性），2 类场景 κ = 0.748（高度一致性）。

**篇章级标签**：通过平均每篇中的句子级标签得到。图 4 显示篇章级得分分布在 +1.0 处有一个大峰值，这些点被称为“完全幻觉”——响应信息与真实概念完全无关，完全由 LLM 编造。

### 3.8 句子级幻觉检测实验结果

实验在三个任务上评估方法：
1. **NonFact**：检测 non-factual 句子（major + minor 合并为非事实类，accurate 为事实类）；
2. **NonFact***：更具挑战性的任务，仅检测 major-inaccurate 句子，且排除完全幻觉篇章（共 206 个篇章、1632 个句子）；
3. **Factual**：检测事实性句子。

**主要观察结果**：

1. **LLM 自身概率与事实性相关性良好**：GPT-3 的 $Avg(-\log p)$ 在 Factual 检测上 AUC-PR 为 53.97，显著优于随机基线 27.04；在 NonFact 检测上从随机的 72.96 提升到 83.21。这验证了“LLM 对生成信息不确定时 token 往往具有更高不确定性”的假设。概率度量优于基于 top-5 token 的熵度量。

2. **代理 LLM 明显劣于原 LLM**：LLaMA-30B 的熵度量优于概率度量，说明更丰富的不确定性信息可以改善检测性能。但 GPT-NeoX 或 OPT-30B 等代理 LLM 的性能接近随机基线。

3. **SelfCheckGPT 全面优于灰盒方法**：SelfCheckGPT-Prompt 在所有任务上显著优于灰盒方法和其他黑盒方法。BERTScore、QA 和 n-gram 变体在多数设置下也优于灰盒方法。

4. **n-gram 变体表现**：Max 变体优于 Avg 变体，且随着 n 增大 Max 性能下降。

5. **NLI 变体接近 Prompt 方法**：在所有黑盒和灰盒基线中表现第二好，且计算效率远优于 Prompt 方法，是实用性最高的方案。

主结果表（表 2）如下：

| 方法 | NonFact | NonFact* | Factual | Pearson | Spearman |
|------|---------|----------|---------|---------|----------|
| Random | 72.96 | 29.72 | 27.04 | — | — |
| GPT-3 Avg(−log p) | 83.21 | 38.89 | 53.97 | 57.04 | 53.93 |
| GPT-3 Avg(H)† | 80.73 | 37.09 | 52.07 | 55.52 | 50.87 |
| GPT-3 Max(−log p) | 87.51 | 35.88 | 50.46 | 57.83 | 55.69 |
| GPT-3 Max(H)† | 85.75 | 32.43 | 50.27 | 52.48 | 49.55 |
| LLaMA-30B Avg(−log p) | 75.43 | 30.32 | 41.29 | 21.72 | 20.20 |
| LLaMA-30B Avg(H) | 80.80 | 39.01 | 42.97 | 33.80 | 39.49 |
| LLaMA-30B Max(−log p) | 74.01 | 27.14 | 31.08 | −22.83 | −22.71 |
| LLaMA-30B Max(H) | 80.92 | 37.32 | 37.90 | 35.57 | 38.94 |
| SelfCk-BERTScore | 81.96 | 45.96 | 44.23 | 58.18 | 55.90 |
| SelfCk-QA | 84.26 | 40.06 | 48.14 | 61.07 | 59.29 |
| SelfCk-Unigram (max) | 85.63 | 41.04 | 58.47 | 64.71 | 64.91 |
| SelfCk-NLI | **92.50** | 45.17 | 66.08 | 74.14 | 73.78 |
| SelfCk-Prompt | **93.42** | **53.19** | **67.09** | **78.32** | **78.30** |

†GPT-3 API 仅返回 top-5 token 的概率用于计算熵。

### 3.9 篇章级事实性排序实验结果

篇章级事实性得分通过平均句子级得分得到（公式 12）。表 2 和图 6 显示，所有 SelfCheckGPT 变体与人类判断的相关性都远超其他基线（包括灰盒概率和熵方法）：

- **SelfCheckGPT-Prompt** 表现最佳，Pearson 相关性 78.32、Spearman 78.30；
- **SelfCheckGPT-NLI** 紧随其后，Pearson 74.14、Spearman 73.78；
- **SelfCheckGPT-Unigram (max)** 为 64.71 / 64.91；
- 灰盒方法中 GPT-3 自身的概率度量最高仅达 57.83 / 55.69；
- 代理 LLM 方法相关性显著较低，甚至出现负相关。

### 3.10 消融实验

**外部知识替代自抽样**（表 3）：使用 WikiBio 中每个概念的第一段（真实维基百科文本）作为知识源，替代自抽样样本进行一致性测量。结果显示：
- BERTScore 和 QA 方法使用自抽样样本时与使用参考段落性能相当，甚至更好；
- n-gram 方法使用 WikiBio 段落时性能大幅下降（Pearson 从 64.71 降至 28.67），因为单篇参考文本不足以训练 n-gram 模型；
- NLI 和 Prompt 方法在获得外部检索信息时能明显受益（Prompt 的 Pearson 从 78.32 提升至 85.90）。但实践中外部数据库不可能覆盖 LLM 生成的所有用例。

**样本数量的影响**（图 7）：所有 SelfCheckGPT 方法的性能随样本数增加而平滑提升，但边际收益递减。n-gram 方法需要最多的样本才能达到性能平台期。句子级检测（图 8）也呈现类似趋势。

**Prompt 评估器的选择**（表 4）：GPT-3 可以自检自身文本；ChatGPT 作为评估器略有提升且成本低 10 倍。

## 4 四、前景与发展方向

### 4.1 扩展到更广泛的概念类型

本文的 238 篇 GPT-3 生成文本主要关于 WikiBio 数据集中的人物传记。未来工作应将研究扩展到更广泛的概念类型，如**地点、物体、事件等**。不同概念类型的知识密度和 LLM 幻觉模式可能不同，这将进一步检验 SelfCheckGPT 框架的普适性，并可能揭示需要针对性改进的幻觉类型。

### 4.2 细粒度原子事实分解

本文在句子级别考虑事实性，但一个句子可能同时包含事实和非事实信息。Min et al.（2023）提出的 FActScore 方法通过将句子分解为原子事实进行细粒度评估。将 SelfCheckGPT 与原子事实分解相结合，可以在**更细粒度上定位幻觉片段**，不仅判断句子是否为幻觉，还能指出具体是哪些事实断言不可靠，这对用户判断和自动纠错都更有价值。

### 4.3 降低 SelfCheckGPT-Prompt 的计算成本

SelfCheckGPT-Prompt 是最优的自检方法，但计算量相当大——需要用 LLM 对每个句子与每个采样样本进行提示评估，API 调用成本高昂（估计 1908 个句子 × 20 个样本的 Yes/No 评估约需 $200 美元使用 GPT-3）。未来工作可以探索：
- 使用更轻量的评估模型（如 ChatGPT 已大幅降低至 $20 美元，但仍需大量调用）；
- 分层抽样策略，优先对高不确定性句子进行 Prompt 评估；
- 将 NLI 变体作为预筛选器，仅对边界案例调用 LLM 提示评估；
- 蒸馏 Prompt 评估器的行为到小型判别模型上。

### 4.4 改进代理LLM方法

本文发现代理 LLM 的简单概率/熵度量性能不佳，原因在于不同 LLM 的生成模式差异。未来可以探索：
- 使用加权条件语言模型分数（如 BARTScore，Yuan et al., 2021）替代原始概率；
- 在代理 LLM 上使用更丰富的熵估计（而非仅 top-5 token）；
- 对代理 LLM 进行少量域内适配以缩小与目标 LLM 的分布差异；
- 结合多个代理 LLM 的集成不确定性估计。

### 4.5 将外部知识与自抽样结合

消融实验（表 3）表明，当外部知识可用时，NLI 和 Prompt 变体可以显著受益（Prompt 的 Pearson 从 78.32 提升至 85.90）。未来工作可以设计**混合架构**：默认使用零资源的自抽样一致性作为基础检测，当检索到高置信度的外部证据时动态增强检测能力。这种设计既能保持零资源场景的通用性，又能在知识密集型任务上获得更精确的评估。

